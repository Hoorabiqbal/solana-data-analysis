# Dependencies for the live Streamlit dashboard (app/live_dashboard.py)
# Note: the dashboard talks to Binance's public REST + WebSocket endpoints
# directly (no API key required), so python-binance is not needed.
streamlit==1.48.1
streamlit-autorefresh
requests==2.32.5
websocket-client==1.8.0
pandas==2.3.2
numpy==2.2.6
plotly==6.3.0
python-dateutil==2.9.0.post0
pytz==2025.2
dateparser==1.2.2
tzlocal==5.3.1
